{
  "name": "zero-video-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "chrome:debug": "bash scripts/start-chrome-debug.sh",
    "dev:web": "npm run build && node dist/web-demo/server.js",
    "serve:web": "node dist/web-demo/server.js",
    "dev:story-video": "npm run build && go run ./cmd/story-video-server",
    "start:story-video-server": "npm run build && go run ./cmd/story-video-server",
    "build:story-video": "go build ./cmd/story-video-server",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "playwright-core": "^1.52.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.15.0",
    "ts-node": "^10.9.2"
  }
}
